#include "shell.h"

/**
 * simple_arithemetic - Simple arithemetic c program
 *
 * Description: print basic arithemetic code
 * of two declared intergers
 *
 * Return: 0 on Success
 */

void simple_arithemetic(void)
{

	int a = 4;
	int b = 2;

	/* arithemetic processes */
	int sum = a + b;
	int subtration = a - b;

	write("sum: %d\n", sum);
	write("subtration: %d\n", subtraction);

	return (0);
}
